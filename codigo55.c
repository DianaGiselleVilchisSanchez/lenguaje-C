#include <stdio.h>
#include <windows.h>

int main() {
    Beep(262, 300); // DO
    Beep(294, 300); // RE
    Beep(330, 300); // MI
    Beep(349, 300); // FA
    Beep(392, 300); // SOL
    Beep(440, 300); // LA
    Beep(494, 300); // SI
    Beep(523, 600); // DO
    return 0;
}